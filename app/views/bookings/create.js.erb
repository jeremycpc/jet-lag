// function refreshForm(innerHTML) {
//   const newBookingForm = document.getElementById('new_booking');
//   newBookingForm.innerHTML = innerHTML;
// }

// // function addBooking(bookingHTML) {
// //   const reviewsList = document.getElementById('reviews');
// //   reviewsList.insertAdjacentHTML('beforeend', bookingHTML);
// // }

// function refreshPrice(countHTML) {
//   const priceCount = document.getElementById('booking_price');
//   const totalPrice = document.getElementById('total-price');
//   totalPrice.innerHTML = countHTML;
//   const seats = document.getElementById('booking_seats')
//   let totalPrice = document.getElementById('total-price')
// }

// <% if @booking.save %>

// refreshForm('<%= j render "bookings/bookingform", jet: @jet, booking: Booking.new %>');
// refreshPrice('<%= @booking.booking_price %>');
// <% else %>
// refreshForm('<%= j render "bookings/bookingform", jet: @jet, booking: @booking %>');
// <% end %>

// seats.addEventListener('input', function(e) {
//   totalPrice.innerText = Number(totalPrice.innerText) * Number(e.target.value)
// });
